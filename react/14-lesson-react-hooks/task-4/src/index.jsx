import React from 'react';
import ReactDOM from 'react-dom';
import ConnectionStatus from './ConnectionStatus';

const root = document.querySelector('#root');

ReactDOM.render(<ConnectionStatus />, root);
