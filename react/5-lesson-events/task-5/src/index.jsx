import React from 'react';
import ReactDOM from 'react-dom';
import './index.scss';
import ButtonStatus from './Toggler';

const rootElement = document.querySelector('#root');
ReactDOM.render(<ButtonStatus />, rootElement);
